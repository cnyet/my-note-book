# Blog 页面设计文档

**对应设计稿**:
- 桌面版: `blog-desktop.png`
- 移动版: `blog-mobile.png`

**页面路由**: `/blog` (列表) / `/blog/[slug]` (详情)

**页面定位**: 技术博客展示，分享经验总结、工作总结、学习笔记

**MVP状态**: ⏳ MVP后迭代 (Week 7-8)

---

## 视觉设计规范 (Genesis + 阅读优化)

| 元素 | 桌面版 (≥1024px) | 移动版 (<768px) |
|------|------------------|-----------------|
| **整体布局** | 文章主体(左70%) + 目录(右30%) | 单栏布局 |
| **背景** | 深渊黑 | 深渊黑 |
| **字体-标题** | 衬线字体 (Serif) - 优雅专业 | 同桌面版 |
| **字体-正文** | 无衬线 (Sans-serif) - 清晰易读 | 同桌面版 |
| **代码块** | 深色背景 + 语法高亮 | 横向滚动 |
| **图片** | 自适应宽度，圆角 | 全宽展示 |

---

## 页面类型

### 1. 博客列表页

**布局**: 垂直卡片列表

**卡片内容**:
- 特色图片 (16:9比例)
- 标题 (衬线字体，大号)
- 作者头像 + 作者名 + 发布日期 + 阅读时间
- 摘要 (2-3行)
- 标签列表

**筛选功能**:
- 按标签筛选
- 按日期筛选
- 全局搜索

---

### 2. 博客详情页

**布局**: 文章主体 + 右侧固定目录

#### 顶部导航

```
┌─────────────────────────────────────────────────────────────┐
│  Logo                Blog | Docs | [Sign In]                │
│                        ▲ 当前选中下划线                      │
└─────────────────────────────────────────────────────────────┘
```

#### 文章头部

- **标题**: 大字号衬线字体
  - 示例: "The Future of AI in Workflow Automation"
  - 字体大小: 桌面 48px / 移动 32px
  - 字体族: Georgia, Times New Roman, serif

- **作者信息**:
  - 作者头像 (圆形，40px)
  - 作者名 (如 "Dr. A. Chen")
  - 发布日期 (如 "Oct 29, 2023")
  - 阅读时间估计 (如 "8 min read")

#### 文章正文

**排版规范**:

| 元素 | 样式 |
|------|------|
| **段落** | 行高1.8，段落间距1.5em，字体16px |
| **标题h2** | 衬线字体，28px，上下margin 1.5em |
| **标题h3** | 衬线字体，22px，上下margin 1em |
| **列表** | 无序列表用圆点，有序列表用数字 |
| **引用块** | 左侧4px青色边框，背景略深 |
| **链接** | 青色(#00f2ff)，悬停下划线 |
| **强调** | 粗体白色，斜体灰色 |

**代码块**:

```typescript
// 示例代码块样式
interface CodeBlockProps {
  language: string;
  code: string;
  showLineNumbers?: boolean;
  copyable?: boolean;
}
```

- 背景: 比正文更深的深色 (#111118)
- 语法高亮:
  - 关键字: 粉色 (#ff006e)
  - 字符串: 绿色 (#10b981)
  - 函数: 蓝色 (#3b82f6)
  - 注释: 灰色 (#6b7280)
- 行号: 可选，灰色侧边栏
- 复制按钮: 右上角，点击反馈

**图片展示**:
- 自适应宽度 (不超过容器)
- 圆角 (8px)
- 阴影效果
- 可选: 点击放大查看

**架构图/流程图**:
- 支持 Mermaid 语法
- 或嵌入 SVG 图片
- 示例: Work-Agents Architecture 流程图

#### 右侧目录 (Table of Contents)

**桌面版专属**

```
┌─────────────────────┐
│ Table of Contents   │
│                     │
│ ■ Introduction      │
│ □ Architecture      │
│ □ Code Examples     │
│ □ Conclusion        │
└─────────────────────┘
```

- 固定在右侧
- 当前阅读位置高亮
- 点击锚点跳转
- 滚动时自动更新

#### 底部互动栏 (移动版)

```
┌─────────────────────────────────┐
│ ♡ Like   💬 Comment  🔖 Bookmark  ↗ Share │
└─────────────────────────────────┘
```

---

## 移动版适配

### 布局变化
- 单栏布局，目录隐藏或折叠
- 顶部返回箭头 + Logo + 分享按钮
- 底部固定互动栏

### 特色图片
- 全宽展示
- 16:9比例
- 视差滚动效果 (可选)

### 代码块
- 横向滚动
- 字体缩小
- 复制按钮常驻

---

## Genesis 组件使用清单

| 组件 | 位置 | 说明 |
|------|------|------|
| `GlassCard` | 文章卡片 | 列表页卡片容器 |
| `GradientText` | 可选 | 特色标题 |
| `CyberGrid` | 可选背景 | 科技感背景 |

---

## 技术实现要点

### Markdown渲染

```tsx
import ReactMarkdown from 'react-markdown';
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';
import { vscDarkPlus } from 'react-syntax-highlighter/dist/esm/styles/prism';

<ReactMarkdown
  components={{
    code({ node, inline, className, children, ...props }) {
      const match = /language-(\w+)/.exec(className || '');
      return !inline && match ? (
        <SyntaxHighlighter
          style={vscDarkPlus}
          language={match[1]}
          PreTag="div"
          {...props}
        >
          {String(children).replace(/\n$/, '')}
        </SyntaxHighlighter>
      ) : (
        <code className={className} {...props}>
          {children}
        </code>
      );
    }
  }}
>
  {markdownContent}
</ReactMarkdown>
```

### 目录生成

```typescript
// 从Markdown提取标题生成目录
const generateToc = (content: string) => {
  const headings = content.match(/^##?\s+(.+)$/gm) || [];
  return headings.map((heading, index) => {
    const level = heading.startsWith('## ') ? 2 : 1;
    const text = heading.replace(/^#+\s+/, '');
    const id = text.toLowerCase().replace(/\s+/g, '-');
    return { level, text, id };
  });
};
```

### 锚点跳转

```tsx
// 平滑滚动到锚点
const scrollToSection = (id: string) => {
  const element = document.getElementById(id);
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
};
```

---

## 富文本编辑器 (管理后台)

### 功能需求

- **Markdown 实时预览**: 分屏编辑
- **语法高亮**: 支持多种编程语言
- **图片上传**: 拖拽上传，自动生成链接
- **代码块**: 一键插入，选择语言
- **表格编辑**: 可视化表格工具

### 不支持的功能
- ❌ 草稿状态
- ❌ 定时发布

### 编辑器推荐
- **Milkdown**: Markdown编辑器，插件丰富
- **Tiptap**: 可定制的富文本编辑器
- **React-Mde**: Markdown编辑器组件

---

## 响应式断点

```css
/* 桌面版 - 双栏 */
@media (min-width: 1024px) {
  .article-container { grid-template-columns: 1fr 300px; }
  .toc { display: block; position: sticky; top: 100px; }
}

/* 平板版 - 单栏，隐藏目录 */
@media (min-width: 768px) and (max-width: 1023px) {
  .article-container { max-width: 700px; margin: 0 auto; }
  .toc { display: none; }
}

/* 移动版 - 全宽 */
@media (max-width: 767px) {
  .article-container { padding: 0 16px; }
  .toc { display: none; }
  .action-bar { position: fixed; bottom: 0; left: 0; right: 0; }
}
```

---

## 相关文档

- [设计规范](../genesis-design-system.md)
- [Home页面](./home.md)
- [Markdown写作指南](../../../docs/development/markdown-guide.md)
