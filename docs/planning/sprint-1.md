# Sprint 1: 后台管理系统设计文档

> **版本**: v1.0
> **状态**: 待实施
> **创建日期**: 2026-02-11
> **关联文档**: [架构设计](../design/architecture.md) | [数据库设计](../design/database-schema.md) | [API 设计](../design/api-design.md)

---

## 1. 项目概述与技术栈

### 1.1 项目概述

为 MyNoteBook 项目构建现代化的后台管理系统，支持智能体配置、博客管理、工具库和实验室管理。后台与前台共享同一个 Next.js 项目，采用前后端分离架构。

### 1.2 核心技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **前端框架** | Next.js 15.5 (App Router) + React 19 | 前台与后台共享 |
| **后端框架** | FastAPI (Python 3.11+) | 独立后端服务 |
| **数据库** | SQLite + better-sqlite3 | 开发环境，生产可切换 PostgreSQL |
| **ORM** | SQLAlchemy 2.0+ | 数据库抽象层 |
| **认证** | JWT + bcrypt | Token 认证 |
| **状态管理** | TanStack Query + Zustand | 服务端 + 客户端状态 |
| **UI 组件** | 独立后台 UI 系统 | 待选定（Ant Design Pro 或 Refine） |
| **主题** | next-themes | 深色/浅色切换 |

### 1.3 路由结构

```
前台:          /home, /agents, /tools, /labs, /blog
后台:          /admin, /admin/login
后台 API:      /api/v1/admin/*
后端 FastAPI:  http://localhost:8001/api/v1/admin/*
```

---

## 2. 架构设计

### 2.1 前后端分离架构

```
┌─────────────────────────────────────────────┐
│           管理后台前端 (Next.js)             │
│           frontend/app/admin/               │
│  - Dashboard, Blog, Agents 管理页面         │
│  - 调用 FastAPI 后端 API                    │
└─────────────────────────────────────────────┘
                    ↓ HTTP/REST API
┌─────────────────────────────────────────────┐
│           FastAPI 后端                      │
│           backend/src/api/v1/admin/         │
│  - 认证中间件 (JWT)                         │
│  - CRUD 业务逻辑                            │
│  - 数据库操作 (SQLAlchemy)                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│           SQLite 数据库                      │
│           backend/data/my_note_book.db      │
└─────────────────────────────────────────────┘
```

### 2.2 职责划分

**前端职责**（`frontend/app/admin/`）：
- UI 组件渲染和用户交互
- 表单验证（React Hook Form + Zod）
- 调用后端 API（fetch 或 axios）
- 状态管理（TanStack Query 缓存）
- 主题切换、路由导航

**后端职责**（`backend/src/api/v1/admin/`）：
- JWT 认证和授权
- CRUD 业务逻辑
- 数据库操作（SQLAlchemy ORM）
- 文件上传处理
- 速率限制和日志记录

---

## 3. 数据库与 API 设计

### 3.1 数据库初始化

- 使用 better-sqlite3 创建数据库文件 `data/admin.db`
- 初始化核心表：`users`（管理员）、`blog_posts`（文章草稿）、`agents`（智能体配置）
- 创建管理员默认账户（用户名：admin，密码首次登录时修改）

### 3.2 API 路由结构

**后端 FastAPI**（`backend/src/api/v1/admin/`）：
```
POST   /api/v1/admin/auth/login       # 管理员登录
GET    /api/v1/admin/auth/verify      # 验证 Token
GET    /api/v1/admin/dashboard/stats  # Dashboard 统计（模拟）
GET    /api/v1/admin/blog             # 博客列表
POST   /api/v1/admin/blog             # 创建文章
GET    /api/v1/admin/blog/{id}        # 文章详情
PUT    /api/v1/admin/blog/{id}        # 更新文章
DELETE /api/v1/admin/blog/{id}        # 删除文章
PATCH  /api/v1/admin/blog/{id}/status # 发布/下架
GET    /api/v1/admin/agents           # 智能体列表
POST   /api/v1/admin/agents           # 创建智能体
GET    /api/v1/admin/agents/{id}      # 智能体详情
PUT    /api/v1/admin/agents/{id}      # 更新智能体
DELETE /api/v1/admin/agents/{id}      # 删除智能体
```

### 3.3 认证流程

```
1. 登录：POST /api/v1/admin/auth/login → 返回 JWT Token
2. 存储：Token 存入 localStorage 和 Cookie（httpOnly）
3. 验证：每个请求携带 Authorization: Bearer {token}
4. 续期：Access Token 过期前用 Refresh Token 自动刷新
```

---

## 4. 页面布局与导航

### 4.1 整体布局

- **左侧侧边栏**（固定 260px 宽）：Genesis 深色系（#0a0a0f 背景）
- **顶部导航栏**（60px 高）：浅色/深色自适应
- **主内容区**：自适应宽度，浅色背景

### 4.2 侧边栏菜单（扁平单层）

```
📊 Dashboard        /admin
🤖 智能体           /admin/agents
🔧 工具库           /admin/tools
🧪 实验室          /admin/labs
📝 博客管理         /admin/blog
👤 个人中心         /admin/profile
⚙️ 系统设置         /admin/settings
```

### 4.3 前端目录结构

```
frontend/app/admin/
├── layout.tsx          # 后台布局（侧边栏+主内容）
├── page.tsx            # Dashboard
├── login/
│   └── page.tsx        # 登录页
├── agents/
│   └── page.tsx        # 智能体管理
├── blog/
│   ├── page.tsx        # 博客列表
│   ├── new/page.tsx    # 新建文章
│   └── [id]/
│       └── page.tsx    # 编辑文章
├── tools/page.tsx      # 工具管理
├── labs/page.tsx       # 实验室管理
├── profile/page.tsx    # 个人中心
└── settings/page.tsx   # 系统设置
```

### 4.4 响应式适配

- 桌面端（≥1024px）：侧边栏常驻
- 平板端（768-1023px）：侧边栏可收起，显示图标
- 移动端（<768px）：侧边栏默认隐藏，汉堡菜单呼出

---

## 5. Dashboard 页面设计

### 5.1 统计卡片（4 个，顶部横向）

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ 总访客量     │ 文章总数     │ 智能体活跃度 │ 系统状态     │
│ 12,458      │ 48 篇       │ 5/5 在线    │ 运行正常     │
│ ↑ 12%       │ +3 本月     │ 实时更新    │ 99.9% 可用  │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

### 5.2 图表区域

- **折线图**：7 日访客流量趋势
- **柱状图**：智能体调用次数统计
- **饼图**：文章分类分布

### 5.3 快捷操作

- "新建文章" 按钮 → `/admin/blog/new`
- "智能体配置" 快捷入口
- "系统清理" 按钮（清理缓存、日志）

### 5.4 数据源

所有数据均为模拟数据，硬编码在后端 API。

---

## 6. Blog 管理设计

### 6.1 列表页 (`/admin/blog`)

**表格视图**：
- 显示字段：标题、摘要、发布日期、状态（草稿/已发布）、操作（编辑/删除）
- 筛选器：状态筛选、搜索框
- 排序：按日期、按标题
- 批量操作：批量发布、批量删除

### 6.2 编辑器 (`/admin/blog/new` 和 `/admin/blog/[id]`)

**功能特性**：
- **双模式切换**：WYSIWYG（Tiptap）↔ Markdown
- **实时预览**：左右分栏，编辑 + 预览
- **元数据表单**：标题、摘要、封面图、SEO 标题、SEO 描述、发布时间
- **状态切换**：保存草稿 / 发布
- **图片上传**：拖拽上传，存储到 `public/uploads/blog/`

### 6.3 发布流程

```
保存草稿 → SQLite (draft)
点击发布 →
  1. SQLite 更新状态 (published)
  2. 生成 Markdown 文件 → content/blog/{slug}.md
  3. 前台重新验证 → 显示新文章
```

---

## 7. 智能体、工具库、实验室管理

### 7.1 智能体管理 (`/admin/agents`)

**列表视图**：卡片网格或表格
- 显示字段：名称、Slug、状态（offline/spawned/idle）、排序、操作
- 支持拖拽排序

**编辑表单**：
```
基本信息：
  - 名称（name）
  - URL 标识（slug）
  - 描述（description）
  - 图标路径（icon_url）

高级配置（JSON config）：
  - 模型选择（Gemini/GPT-4/其他）
  - Prompt 模板
  - 调用限额
  - WebSocket 优先级

连接配置：
  - LobeChat 助手链接（external_url）
  - 内部 API 端点（api_endpoint）
```

### 7.2 工具库管理 (`/admin/tools`)

- 列表展示：名称、分类（Dev/Auto/Intel/Creative）、状态、排序
- 支持拖拽排序、分类筛选
- 编辑：名称、描述、图标、链接、SEO 元数据

### 7.3 实验室管理 (`/admin/labs`)

- 列表展示：名称、状态（Experimental/Preview/Archived）、在线人数
- 编辑：名称、描述、Demo 链接、媒体资源
- 状态切换：Experimental ↔ Preview ↔ Live

---

## 8. 个人中心与系统设置

### 8.1 个人中心 (`/admin/profile`)

**表单字段**：
```
基本信息：
  - 用户名（username，只读）
  - 邮箱（email）
  - 显示名称

密码修改：
  - 当前密码（验证用）
  - 新密码（确认两次）
  - 密码强度指示器

API Token：
  - 生成新 Token 按钮
  - Token 列表（过期时间、撤销按钮）
```

### 8.2 系统设置 (`/admin/settings`)

**配置分组**：

**常规设置**：站点标题、描述、Logo 上传、时区

**内容设置**：每页文章数量、自动保存间隔、Markdown 存储路径

**智能体设置**：默认模型、调用超时、WebSocket 心跳间隔

**安全设置**：Session 过期时间、最大登录尝试次数、IP 白名单

**数据管理**：数据库备份、清理缓存、清理日志、重置默认设置

---

## 9. 主题切换与样式系统

### 9.1 双主题设计

- **侧边栏**：固定 Genesis 深色系（#0a0a0f 背景），不受主题切换影响
- **顶部导航 + 内容区**：支持浅色/深色切换

### 9.2 主题定义

```javascript
colors: {
  admin: {
    light: {
      bg: '#f8fafc',      // slate-50
      surface: '#ffffff', // white
      border: '#e2e8f0',  // slate-200
      text: '#0f172a',    // slate-900
    },
    dark: {
      bg: '#0f172a',      // slate-900
      surface: '#1e293b', // slate-800
      border: '#334155',  // slate-700
      text: '#f1f5f9',    // slate-100
    }
  }
}
```

### 9.3 主题持久化

- 存储位置：`localStorage` (`admin-theme`)
- 默认值：浅色（`light`）
- 应用范围：仅后台 `/admin/*`

---

## 10. 实施计划

### 10.1 开发阶段

**Phase 1: 基础设施（Week 1）**
- 认证 API（登录、Token 验证）
- 后台布局（侧边栏、导航）
- 登录页面
- 路由保护中间件

**Phase 2: Dashboard + Blog 管理（Week 2）**（MVP）
- Dashboard 页面（统计卡片、图表）
- Blog 列表页
- Blog 编辑器（Tiptap + Markdown）
- Blog CRUD API

**Phase 3: 智能体、工具、实验室管理（Week 3）**
- Agents 管理页面
- Tools 管理页面
- Labs 管理页面
- 对应 CRUD API

**Phase 4: 个人中心、系统设置、优化（Week 4）**
- 个人中心页面
- 系统设置页面
- 媒体上传功能
- 性能优化和测试

### 10.2 技术债务与后续优化

- WebSocket 实时更新（智能体状态）
- 前端独立 UI 系统选定
- 数据库迁移到 PostgreSQL（高并发场景）
- E2E 测试覆盖

---

**文档版本**: v1.0
**最后更新**: 2026-02-11
**状态**: 待实施

**实施计划**: [2026-02-11-admin-dashboard-implementation.md](../implements/2026-02-11-admin-dashboard-implementation.md)
