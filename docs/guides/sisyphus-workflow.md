# Sisyphus 工作流指南

> OhMyOpenCode 项目管理系统的核心概念和使用指南

## 概述

`.sisyphus/` 是 OhMyOpenCode 工作流系统的专用目录，用于项目管理、学习记录和规划。它充当 AI Agent 之间的**共享知识库**，实现跨会话的记忆累积和协作。

## 目录结构

```
.sisyphus/
├── plans/           # 📋 实施计划（官方计划文件，只读）
│   └── work-agents-mvp.md    # 项目主计划
├── notepads/        # 📝 学习笔记（累积知识，可追加）
│   └── work-agents-mvp/
│       ├── learnings.md      # 模式、约定、成功方法
│       ├── issues.md         # 问题、阻塞、陷阱
│       ├── decisions.md      # 架构决策和理由
│       └── problems.md       # 未解决的问题、技术债务
└── drafts/          # 📄 草稿（临时探索性内容）
    ├── planning.md
    └── research.md
```

---

## 目录详解

### 1. plans/ - 实施计划

**作用**: 存放官方项目计划，包含任务清单、阶段划分、验收标准。

**特点**:
- **神圣不可侵犯**: 只读文件，Agent 不可修改
- **Orchestrator 专属**: 只有 Atlas（协调者）有权更新
- **单一来源**: 所有任务以计划为准

**文件示例**:
```markdown
# work-agents-mvp.md

## 阶段 1: 基础设施搭建

- [ ] 初始化 Next.js 项目
- [ ] 配置 TypeScript
- [ ] 安装 Shadcn/UI

**验收标准**:
- [ ] http://localhost:3000 可访问
- [ ] 无编译错误
```

---

### 2. notepads/ - 学习笔记

**作用**: 累积跨会话的知识、经验、决策，避免重复踩坑。

**特点**:
- **追加写入**: 永远只添加，不覆盖
- **跨会话共享**: 关闭再打开，知识仍在
- **分类清晰**: learnings、issues、decisions、problems

**4 种笔记类型**:

| 文件 | 内容 | 示例 |
|------|------|------|
| `learnings.md` | 成功的模式和方法 | "使用 Shadcn/UI 后包体积减少 80%" |
| `issues.md` | 遇到的问题和解决方案 | "Phase 2 发现 OAuth 配置缺失，已补充" |
| `decisions.md` | 架构决策和理由 | "选择 Shadcn/UI 而非 Bootstrap 5，因为..." |
| `problems.md` | 待解决的技术债务 | "WebSocket 计数器尚未实现，需 Phase 6 处理" |

**格式规范**:
```markdown
## [TIMESTAMP] Task: {task-id}
{content}
```

---

### 3. drafts/ - 草稿目录

**作用**: 存放探索性、临时性的思考内容。

**特点**:
- 随时可创建、删除
- 供 Prometheus（规划者）使用
- 验证后可移动到 plans/ 或删除

---

## Agent 关系图

### Agent 角色矩阵

| Agent | 角色 | 主要职责 | 与 .sisyphus/ 的关系 |
|-------|------|----------|---------------------|
| **Prometheus** | 规划者 | 制定实施计划 | 读写 `plans/` 创建计划 |
| **Momus** | 审核者 | 审查计划质量 | 读取 `plans/` 进行审核 |
| **Oracle** | 顾问 | 提供技术建议 | 读取作为参考，输出建议 |
| **Metis** | 风险评估 | 识别风险和漏洞 | 读取计划，输出到 `notepads/issues.md` |
| **Atlas** | 协调者 | 协调任务执行 | 协调所有 agent，管理 notepads 知识积累 |
| **Sisyphus** | 执行者 | 具体开发任务 | 读取计划，执行任务，更新 notepads |
| **General** | 总结者 | 任务结果汇总 | 读取任务结果，输出总结报告 |
| **Hephaestus** | 工具 | 提供工具 | 读取作为参考，输出建议 |

---

### 工作流程图

```
                    ┌─────────────┐
                    │   用户需求   │
                    └──────┬──────┘
                           │
                           ↓
              ┌────────────────────────┐
              │      Prometheus        │
              │      (制定计划)         │────→ .sisyphus/plans/*.md
              └───────────┬────────────┘
                          │
                          │ 审核
                          ↓
              ┌────────────────────────┐
              │       Momus            │────→ 提出改进建议
              │      (审核计划)         │
              └───────────┬────────────┘
                          │
                          │ 评估
                          ↓
              ┌────────────────────────┐
              │        Metis           │────→ 风险报告 → notepads/issues.md
              │    (风险评估)           │
              └───────────┬────────────┘
                          │
                          │ 协调
                          ↓
              ┌────────────────────────┐
              │        Atlas           │────→ 执行任务，累积知识
              │      (协调执行)         │────→ 更新 notepads/
              └───────────┬────────────┘
                          │
                          ↓
              ┌────────────────────────┐
              │      Sisyphus          │────→ 实际开发
              │      (执行者)           │────→ 读取计划，执行任务
              └────────────────────────┘
```

---

## 核心原则

### 1. Plans 是神圣的

```
✅ 可以: 读取 plans/ 理解任务
❌ 禁止: 修改 plans/ 文件
✅ 只能: Orchestrator (Atlas) 有权更新计划
```

**原因**:
- 避免多人修改导致冲突
- 保证计划的一致性
- 明确责任归属

---

### 2. Notepads 是累积的

```
✅ 正确: 追加新内容到 notepad
❌ 错误: 覆盖已有内容
✅ 正确: 多个 agent 共同维护
```

**原因**:
- 保留历史记录，便于追溯
- 跨会话知识共享
- 避免重复踩坑

---

### 3. Drafts 是临时的

```
✅ 可以: 创建 drafts/ 探索思路
✅ 可以: 删除不再需要的草稿
✅ 可以: 验证后将草稿移动到 plans/
```

**原因**:
- 保持工作区整洁
- 区分已完成和探索中的内容
- 快速迭代，不被临时思路干扰

---

## 使用示例

### 示例 1: 创建新计划

```markdown
# Prometheus 创建计划

1. 读取现有计划结构
2. 在 drafts/ 创建草稿
3. 验证后移动到 plans/
4. 通知 Momus 进行审核
```

---

### 示例 2: 记录学习到的经验

```markdown
# Sisyphus 完成任务后追加

## 2026-01-31 Task: setup-nextjs
- 发现 Shadcn/UI 安装需要先配置 Tailwind
- 解决方案: 先运行 npx tailwindcss init
```

---

### 示例 3: Metis 输出风险评估

```markdown
# Metis 风险报告

## 高风险项
| 风险 | 影响 | 建议 |
|------|------|------|
| 工期低估 | 可能延期 | 拆分 Phase 4/5 |

→ 追加到 .sisyphus/notepads/work-agents-mvp/issues.md
```

---

## 最佳实践

### 1. 保持 notepads 更新

```bash
# 完成任务后
echo "## [TIMESTAMP] Task: $TASK_ID" >> learnings.md
echo "- 成功模式: ..." >> learnings.md
```

---

### 2. 及时记录问题

```bash
# 遇到阻塞时
echo "## [TIMESTAMP] Issue: $ISSUE" >> issues.md
echo "- 问题描述: ..." >> issues.md
echo "- 临时方案: ..." >> issues.md
```

---

### 3. 定期同步计划

```bash
# 计划变更时
cp docs/implement/implement-plan.md .sisyphus/plans/work-agents-mvp.md
```

---

## 常见问题

### Q1: Agent 可以修改 plans/ 吗？

**不可以**。Plans 是只读的，只有 Orchestrator（Atlas）有权修改。如果需要变更计划，通过 Momus 审核后由 Atlas 更新。

---

### Q2: Notepads 会无限增长吗？

理论上会。建议定期归档：
- 将已完成任务的记录移到单独的归档文件
- 定期清理过时的信息

---

### Q3: 如何处理多个 agent 同时写入 notepad？

Notepads 设计为追加写入，不同 agent 写入不同区块，不会冲突。但建议在写入前先阅读已有内容，避免重复。

---

### Q4: Drafts 和 Notepads 有什么区别？

| 特性 | Drafts | Notepads |
|------|--------|----------|
| 目的 | 探索性思考 | 累积知识 |
| 生命周期 | 临时，可删除 | 长期保留 |
| 访问 | 仅规划者 | 所有 agent |
| 格式 | 自由 | 规范格式 |

---

## 相关文档

- [项目实施计划](../implement/implement-plan.md)
- [架构设计](../design/architecture.md)
- [数据库设计](../design/database-schema.md)
- [API 设计](../design/api-design.md)

---

**最后更新**: 2026-01-31
